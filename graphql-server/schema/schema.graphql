type Query {
  user(id: ID!): User
  post(id: ID!): Posts
  posts: [Posts]
}

type Posts {
  id: ID!
  title: String!
  description: String
  likes: Int
  comments: Int
  views: Int
  user: User
}

type User {
  id: ID!
  name: String!
  followers: Int
  posts: [Posts]
}
